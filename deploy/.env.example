# Domain and routing
BASE_DOMAIN=example.com

# ACME / certificates
ACME_EMAIL=admin@example.com
ACME_DNS_PROVIDER=cloudflare

# DNS provider credentials (set only what your provider uses)
CF_DNS_API_TOKEN=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# Database
POSTGRES_PASSWORD=change-me

# OIDC generic
OIDC_ISSUER=https://auth.example.com/realms/homelab
OIDC_CLIENT_ID=traefik-controlplane
OIDC_CLIENT_SECRET=change-me

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Microsoft OAuth
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common

# Local fallback auth
LOCAL_AUTH_ENABLED=true
INITIAL_ADMIN_USERNAME=admin
# Use bcrypt/argon2 hash, not plaintext
INITIAL_ADMIN_PASSWORD_HASH=

# Internal service auth
AGENT_SHARED_TOKEN=change-this-long-random-token
